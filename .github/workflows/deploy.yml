name: Build & Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build Frontend
        working-directory: app
        run: |
          npm ci
          npm run build

      - name: Deploy Frontend
        run: |
          rm -rf /home/jeremy/dev/financeHub/app/dist/*
          cp -r app/dist/* /home/jeremy/dev/financeHub/app/dist/

      - name: Build Backend
        working-directory: backend
        run: |
          chmod +x gradlew
          ./gradlew bootJar -x test

      - name: Deploy Backend
        run: |
          cp backend/build/libs/*.jar /home/jeremy/dev/financeHub/backend/app.jar
          sudo systemctl daemon-reload
          sudo systemctl restart financehub-backend
          sleep 5
          sudo systemctl is-active --quiet financehub-backend && echo "Backend started successfully" || (echo "Backend failed to start" && exit 1)

      - name: Reload Nginx
        run: docker exec nginx nginx -s reload
